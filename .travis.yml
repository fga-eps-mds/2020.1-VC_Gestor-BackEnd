language: node_js
node_js:
    - "node"

notifications:
    email: false

branches:
    only:
        - master
        - development
        - unit_tests
        - Issue_128-ResetSenha

matrix:
    include:
        - env: TARGET=packages/resolution
        - env: TARGET=packages/user
        - env: TARGET=packages/benefits
        - env: TARGET=packages/news

script: cd $TARGET && npm install && npm run test;